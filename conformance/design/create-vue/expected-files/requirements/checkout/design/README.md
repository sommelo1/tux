# checkout (vue design)

```bash
npm install
tux design start-review --foreground --dir .  # serves the design with TUX review
# or: npm run dev + tux live start-review --url http://localhost:5173
```
